<div class="tabs">
  <div class="tab tab-active" id="tab-1">
    <i class="fas fa-list-alt"></i><br>
    My Pokemons
  </div>
  <div class="tab" id="tab-2">
    <i class="fas fa-calendar-plus"></i><br>
    My Bookings
  </div>
  <div class="tab" id="tab-3">
    <i class="fas fa-star"></i><br>
    My Reviews</div>
  </div>

<div class="container">
  <h1>Welcome <%= current_user.first_name %></h1>

  <%# To do : add curent_user name once model updated  %>

  <div class="content displayed" id="content-1">
    <% @pokemons.each do |pokemon| %>
    <div class="pokemon-card">
      <% if pokemon.photo.attached? %>
        <%= cl_image_tag pokemon.photo.key %>
      <%else%>
        <%= image_tag "default_photo.png" %>
      <%end%>
      <p><%= pokemon.name %></p>
      <p><%= pokemon.description %></p>
      <p><%= pokemon.special_attack %></p>
      <p><%= pokemon.health_point %></p>
      <p><%= pokemon.price_per_day%>€</p>
    <% end %>
    </div>
  </div>

  <div class="content hidden" id="content-2">
      <% @bookings.each do |booking| %>
      <%= booking.start_date %>
      <%= booking.end_date %>
      <%= booking.total_price %>
    <% end %>
    WIP: Bookings
  </div>

  <div class="content hidden" id="content-3">
    WIP: Reviews
  </div>
</div>


